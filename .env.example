# Riot API Key - get from https://developer.riotgames.com/
RIOT_API_KEY=your-riot-api-key-here

# API key for downloading datasets (set a secure random string)
DATASET_API_KEY=change-me-in-production

# Server port
PORT=3000

# Data directory (for SQLite database)
# Leave unset to use ./data locally; Docker sets /app/data automatically
# DATA_DIR=/app/data

# Maximum database size in GB before pausing scraping (default: 16)
# MAX_DB_SIZE_GB=16
